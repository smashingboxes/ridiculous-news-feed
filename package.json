{
  "name": "offensive-news-feed",
  "version": "1.0.0",
  "description": "An offensive news feed based off of real news.",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "start:dev": "nodemon index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploywhileonserver": "git fetch --all && git reset --hard origin/master && pm2 restart 1 && pm2 logs 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.13.4",
    "request": "^2.69.0",
    "xml2js": "^0.4.16"
  },
  "devDependencies": {
    "nodemon": "^1.9.1"
  }
}
